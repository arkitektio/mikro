mutation CreateDimensionMap(
  $omero: ID!
  $dim: Dimension!
  $index: Int!
  $channel: ID
) {
  createDimensionMap(
    omero: $omero
    dim: $dim
    index: $index
    channel: $channel
  ) {
    ...DimensionMap
  }
}
