mutation create_thumbnail(
  $rep: ID!
  $file: ImageFile!
  $major_color: String
  $blurhash: String
) {
  uploadThumbnail(
    rep: $rep
    file: $file
    majorColor: $major_color
    blurhash: $blurhash
  ) {
    ...Thumbnail
  }
}
