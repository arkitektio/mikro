mutation create_metric(
  $rep: ID
  $sample: ID
  $experiment: ID
  $key: String!
  $value: MetricValue!
) {
  createMetric(
    rep: $rep
    sample: $sample
    experiment: $experiment
    key: $key
    value: $value
  ) {
    ...Metric
  }
}
