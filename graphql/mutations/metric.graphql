mutation create_metric(
  $representation: ID
  $sample: ID
  $experiment: ID
  $key: String!
  $value: MetricValue!
) {
  createMetric(
    representation: $representation
    sample: $sample
    experiment: $experiment
    key: $key
    value: $value
  ) {
    ...Metric
  }
}
