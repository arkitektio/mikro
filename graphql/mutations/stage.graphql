mutation create_stage(
  $name: String!
  $creator: ID
  $tags: [String]
  $physical_size: [Float]!
) {
  createStage(
    name: $name
    creator: $creator
    tags: $tags
    physicalSize: $physical_size
  ) {
    ...Stage
  }
}
