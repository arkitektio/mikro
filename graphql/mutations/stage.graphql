mutation create_stage(
  $name: String!
  $creator: ID
  $instrument: ID
  $tags: [String]
) {
  createStage(
    name: $name
    creator: $creator
    instrument: $instrument
    tags: $tags
  ) {
    ...Stage
  }
}
