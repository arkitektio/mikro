mutation create_feature(
  $label: ID!
  $key: String
  $value: FeatureValue!
  $creator: ID
) {
  createfeature(label: $label, key: $key, value: $value, creator: $creator) {
    id
    key
    value
    label {
      id
      representation {
        id
      }
    }
  }
}
