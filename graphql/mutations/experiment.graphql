mutation create_experiment(
  $name: String!
  $creator: String
  $description: String
  $tags: [String]
) {
  createExperiment(
    name: $name
    creator: $creator
    description: $description
    tags: $tags
  ) {
    ...Experiment
  }
}
