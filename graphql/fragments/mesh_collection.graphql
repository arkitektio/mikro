fragment MeshCollection on MeshCollection {
  id
  version
  specVersion
  grid
  encoding
  coordinateSystem {
    ...CoordinateSystem
  }
  store {
    ...FabriksStore
  }
  derivedFrom {
    ...Transformation
  }
}
