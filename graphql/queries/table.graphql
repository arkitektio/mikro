query get_table($id: ID!) {
  table(id: $id) {
    ...Table
  }
}

query expand_table($id: ID!) {
  table(id: $id) {
    ...Table
  }
}
query search_tables {
  tables {
    id: id
    label: name
  }
}
