query get_table($id: ID!) {
  table(id: $id) {
    ...Table
  }
}

query expand_table($id: ID!) {
  table(id: $id) {
    ...Table
  }
}
query search_tables {
  options: tables {
    value: id
    label: name
  }
}
