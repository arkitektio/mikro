query expand_feature($id: ID!) {
  feature(id: $id) {
    ...Feature
  }
}

query search_features($search: String) {
  options: features(substring: $search, limit: 20) {
    label: key
    value: id
  }
}
